name: Flowzone

on:
  pull_request:
    types: [opened, synchronize, closed]
    branches:
      - 'master'
      - 'main'

jobs:
  flowzone:
    name: Flowzone
    uses: drskullster/flowzone/.github/workflows/flowzone.yml@drskullster/add-comment-with-publish-url
    secrets: inherit
    with:
      cloudflare_website: balena-design-system
      docusaurus_website: false
